<div class="max-w-m rounded overflow-hidden shadow-lg my-3">
</div>
<div class="bg-white">
  <div class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 px-4 sm:px-6 lg:max-w-7xl lg:grid-cols-2 lg:px-8">
    <div>
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl"><%= report_item.title %></h2>
      <p class="mt-4 text-gray-500"><%= report_item.remarks %></p>

      <dl class="mt-16 grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 sm:gap-y-16 lg:gap-x-8">
        <div class="border-t border-gray-200 pt-4">
          <dt class="font-medium text-gray-900">Severity</dt>
          <dd class="mt-2 text-sm text-gray-500">
            <span class="inline-flex items-center rounded-md bg-red-50 px-2 py-1 text-xs font-medium text-red-700 ring-1 ring-inset ring-red-600/10"> 
              <%= report_item.severity.titleize %>
            </span>
          </dd>
        </div>
        <div class="border-t border-gray-200 pt-4">
          <dt class="font-medium text-gray-900">Coverage</dt>
          <dd class="mt-2 text-sm text-gray-500"><%= report_item.inspection_coverage.titleize %></dd>
        </div>
        <div class="border-t border-gray-200 pt-4 col-span-2">
          <dt class="font-medium text-gray-900">Notes</dt>
          <dd class="mt-2 text-sm text-gray-500"><%= report_item.notes %></dd>
        </div>
      </dl>
    </div>
    <div class="grid grid-cols-2 grid-rows-2 gap-4 sm:gap-6 lg:gap-8">
      <% report_item.item_images.each do |item_image| %>
        <%= image_tag item_image %>
      <% end %>
    </div>
  </div>
</div>
